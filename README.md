# AI-Powered Photo Gallery

Android private media workspace prototype.

Read `PROJECT_CONTEXT.md` before changing product behavior, then inspect the current code.
Read `TESTING_PLAN.md` before running device tests: instrumentation tests must use a
disposable emulator because reinstalling the app can remove its private media workspace.
